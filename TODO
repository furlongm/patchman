fix database race conditions with django transaction support
use querysets to update/remove packages from repos
install certs on clients, require tinyca? 
use django tags for host tags
fix web interface css (content_block in andsome)
andsome paginator
refactor to re-use code between scripts and models
check database integrity script
prettify repo urls
allows repo urls to be added without e.g. binary-amd64
add descriptions?
fix pakiti2-import script
fix patchman-tool spacing for -p -v
packaages not in any repo on a host
