#!/usr/bin/make -f
# -*- makefile -*-

DEB_PYTHON2_MODULE_PACKAGES := patchman

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/python-distutils.mk

clean::
	rm -rf build MANIFEST dist
	dh_clean

